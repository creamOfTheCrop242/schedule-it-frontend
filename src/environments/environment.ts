export const environment = {
  production: true,
  baseUrl: `https://api.theguidelog.com`,
  /** Set only in non-production builds; used for QA onboarding reset. */
  devOnboardingResetSecret: undefined as string | undefined,
};
